# Fontes legadas

- `../index.html` é a app de gestão original e não deve ser alterada durante a migração.
- `valverde_algarve_2.html` é copiado da entrega original pelo script `npm run extract:legacy-assets`.

As aplicações novas usam apenas os assets extraídos e código TypeScript.
